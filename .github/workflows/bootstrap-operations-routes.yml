name: Bootstrap operations route refactor

on:
  pull_request:
    branches: [Test]
    paths:
      - "scripts/fix_operations_main_preprocessor.py"
      - ".github/workflows/bootstrap-operations-routes.yml"

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-operations-routes
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Correct operations integration boundary
        run: python scripts/fix_operations_main_preprocessor.py

      - name: Validate transformed sources
        run: |
          python -m py_compile \
            scripts/auth_contract_tests.py \
            backend/tests/operations_routes_boundary_tests.py
          python backend/tests/operations_routes_boundary_tests.py

      - name: Commit corrected operations integration
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/src/main.cpp
          git diff --cached --check
          git commit -m "Correct operations route integration boundary"
          git push origin HEAD:refactor/extract-operations-routes
