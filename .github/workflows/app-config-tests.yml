name: Backend core unit tests

on:
  push:
    branches: [refactor/extract-league-routes]
    paths:
      - "scripts/refactor_league_routes.py"
      - ".github/workflows/app-config-tests.yml"
  pull_request:
    branches: [Test]
    paths:
      - "scripts/refactor_league_routes.py"
      - ".github/workflows/app-config-tests.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bootstrap-league-routes:
    name: Apply league route extraction
    if: (github.ref == 'refs/heads/refactor/extract-league-routes' || github.head_ref == 'refactor/extract-league-routes') && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-league-routes
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Correct generated-boundary escaping
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path("scripts/refactor_league_routes.py")
          text = path.read_text(encoding="utf-8")
          start = text.index("boundary = f'''")
          end = text.index("BOUNDARY_PATH.write_text", start)
          block = text[start:end]
          for name in (
              "path",
              "handler",
              "methods",
              "expected_count",
              "actual_count",
              "preflight_name",
              "contract",
          ):
              block = block.replace("{" + name + "}", "{{" + name + "}}")
          block = block.replace('path.count("{")', 'path.count("{{")')
          block = block.replace(
              'routes.count("{drogon::Options}")',
              'routes.count("{{drogon::Options}}")',
          )
          path.write_text(text[:start] + block + text[end:], encoding="utf-8")
          PY
      - name: Apply exact league route transformation
        run: |
          python -m py_compile scripts/refactor_league_routes.py
          python scripts/refactor_league_routes.py
      - name: Validate generated route boundaries
        run: |
          python -m py_compile \
            scripts/auth_contract_tests.py \
            backend/tests/http_security_boundary_tests.py \
            backend/tests/league_routes_boundary_tests.py
          python backend/tests/http_security_boundary_tests.py
          python backend/tests/league_routes_boundary_tests.py
      - name: Commit generated league route integration
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            backend/src/main.cpp \
            backend/src/league_routes.h \
            backend/src/league_routes.cpp \
            backend/CMakeLists.txt \
            backend/tests/league_routes_boundary_tests.py \
            backend/tests/http_security_boundary_tests.py \
            scripts/auth_contract_tests.py
          git diff --cached --check
          git commit -m "Extract league route registration"
          git push origin HEAD:refactor/extract-league-routes
